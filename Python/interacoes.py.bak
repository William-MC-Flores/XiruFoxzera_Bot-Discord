from discord.ext import commands

class Interacoes(commands.Cog):
    def __init__(self, bot):
        self.bot = bot

    @commands.Cog.listener()
    async def on_message(self, message):
        if message.author.bot:
            return

        conteudo = message.content.lower()

        # responder a certas mensagens
        if "opa" in conteudo:
            await message.channel.send("🙋‍♂️ Aoba!!")
        elif "boa noite" in conteudo:
            await message.channel.send("🌙 Boa noite")
        elif "salve" in conteudo:
            await message.channel.send("🤙 Salve fih")
        elif "bão fih?" in conteudo:
            await message.channel.send("😎 Bão fih")
        elif "leva nabunda ou deixa nabunda?" in conteudo:
            await message.channel.send("🤨 nabunda nada")
        elif "que time é teu?" in conteudo:
            await message.channel.send("🤨 bateu na trave, entrou no teu")
        elif "nem te conto" in conteudo:
            await message.channel.send("👀 Opa, fofoca?")
        elif "sigma" in conteudo:
            await message.channel.send("🧏‍♂️ bye bye")
        elif "bom dia" in conteudo:
            await message.channel.send("🌞 Bom dia")
        elif "buenas?" in conteudo:
            await message.channel.send("🙋‍♂️ buenas")

        # adicionar reações em certas mensagens
        elif "suave" in conteudo:
            await message.add_reaction("🤙")
        elif "te odeio" in conteudo:
            await message.add_reaction("💔")

async def setup(bot):
    await bot.add_cog(Interacoes(bot))
